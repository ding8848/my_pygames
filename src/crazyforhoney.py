import sys, pygame

pygame.init()
size = weight, height = 600, 800
screen = pygame.display.set_mode(size)
pygame.display.set_caption("Crazy for honey")

while 1:ßß
    for event in pygame.event.get():
        if event.type == pygame.QUIT: sys.exit()
